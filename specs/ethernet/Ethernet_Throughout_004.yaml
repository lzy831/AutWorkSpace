domain: ethernet
id: Ethernet_Throughout_004
name: TCP 100M-TX
testbed: ethernet_router_lan
category: Performance
auto_status: pending  # pending: iperf-based throughput test, not yet implemented
description: TCP 100M-TX

setup_hooks: []
teardown_hooks: []

# Excel raw data:
#   Priority: P1
#   Preset: 
#   Steps: 1. PC is the server side, enter the command: iperf -s
2. The public version enters the command for the client: iperf -c 192.168.1.3 -i 2 -w 100M -t 300
#   Expected: 90Mbits/sec

preconditions:
  - description: TODO - ensure DUT & PC connected via specified speed (10M/100M/1000M)

procedure:
  - operation:
      description: TODO - iperf test
      api: TODO
