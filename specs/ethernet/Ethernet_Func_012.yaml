domain: ethernet
id: Ethernet_Func_012
auto_status: pending  # pending
name: Static Connect static IP. can the box standby wake up automatically reconnect?
testbed: ethernet_router_lan
description: （概要）

setup_hooks: []
teardown_hooks: []
验证静态 IP 模式下 DUT 待机唤醒后能自动恢复网络连接，验证：1. 待机后 IP 断开；2. 唤醒后静态 IP 恢复。

Preconditions（前置条件/环境准备）
1. DUT 已配置静态 IP 并成功联网。

Steps（操作步骤）
Step 1：DUT 进入深度待机
目的：触发深度待机，测试网络恢复能力。
对应函数：step_enter_standby()
Step 2

# Excel raw data for review:
#   Test_Item: Static
#   Test_SubItem: Connect static IP. can the box standby wake up automatically reconnect?
#   Priority: P0
#   Preset: The box has successfully obtained an IP address and can access the Internet.
#   Steps: 1. Set the DUT IP address to static
2. Press power key on remote
3. After DUT  enter deep sleep status,press power key to wake up the DUT
#   Expected: 1. After the standby. the box network is disconnected. After waking up. you can regain the IP address and access the Internet.
#   API_Summary: [{"kind": "step", "function_name": "step_enter_standby", "description": "使 DUT 进入完全待机/深度休眠状态。", "category": "ethernet_static", "tags": ["kind:step", "domain:ethernet_static"], "params": {}, "pre_condi

preconditions:
  - description: 确保 DUT 处于唤醒状态再开始测试
    operation:
      api: step_ensure_awake
      params:
        max_retries: 3

  - description: DUT以太网物理链路已连通
    operation:
      api: step_ensure_router_lan_up
    check:
      api: check_ethernet_connected

  - description: TODO - based on test preset: The box has successfully obtained an IP address and can access the Internet.

procedure:
  # TODO - review and implement based on Excel steps
  - operation:
      description: TODO
      automation:
        api: TODO
