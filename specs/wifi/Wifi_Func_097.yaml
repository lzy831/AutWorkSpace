_schema: test_case_spec_v1

id: Wifi_Func_097
name: WiFi DLNA 视频播放验证
priority: P1
domain: wifi
environment: wifi_router_lan
description: |
  DLNA

_source:
  file: wifi&BT_testcase_v1.1.xlsx
  sheet: Wifi_function
  case_id: Wifi_Func_097
preconditions:
  - description: 1. 平台已正常启动并进入系统 2. WiFi 模块已初始化
    manual: QA 人工确认
    automation:
      api: check_wifi_connected

steps:
  - operation:
      description: 通过PC建立DLNA服务, 用于后续DLNA播放测试
      manual:
        actor: QA
        target: DUT
        action: 通过PC建立DLNA服务, 用于后续DLNA播放测试
      automation:
        api: step_setup_dlna_server
        params: {}
    check:
      description: 验证WiFi DLNA场景下平台可以流畅播放视频
      manual:
        actor: QA
        target: DUT屏幕
        action: 验证WiFi DLNA场景下平台可以流畅播放视频
      automation:
        api: check_dlna_video_normal
        params: {}

