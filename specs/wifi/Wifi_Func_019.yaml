_schema: test_case_spec_v1

id: Wifi_Func_019
name: Connect AP场景下AP2上电后自动更新状态
priority: P1
domain: wifi
environment: wifi_router_lan
description: |
  Connect AP

_source:
  file: wifi&BT_testcase_v1.1.xlsx
  sheet: Wifi_function
  case_id: Wifi_Func_019
preconditions:
  - description: Multiple APs
    manual: QA 人工确认
    automation:
      api: check_wifi_connected

steps:
  - operation:
      description: AP2 重新上电, 完成'AP2 power on'测试操作
      manual:
        actor: QA
        target: DUT
        action: AP2 重新上电, 完成'AP2 power on'测试操作
      automation:
        api: step_power_on_ap
        params: {}
    check:
      description: 检查 AP 列表界面中 AP2 自动更新状态, 帮助文本变为 'saved'
      manual:
        actor: QA
        target: DUT屏幕
        action: 检查 AP 列表界面中 AP2 自动更新状态, 帮助文本变为 'saved'
      automation:
        api: check_ap_list_display
        params: {}

