_schema: test_case_spec_v1

id: Wifi_Func_089
name: WiFi热点开启及状态检查
priority: P1
domain: wifi
environment: wifi_router_lan
description: |
  Wifi hotspot

_source:
  file: wifi&BT_testcase_v1.1.xlsx
  sheet: Wifi_function
  case_id: Wifi_Func_089
preconditions:
  - description: 1. 平台已正常启动并进入系统 2. WiFi 模块已初始化
    manual: QA 人工确认
    automation:
      api: check_wifi_connected

steps:
  - operation:
      description: 开启WiFi热点
      manual:
        actor: QA
        target: DUT
        action: 开启WiFi热点
      automation:
        api: step_enable_wifi_hotspot
        params: {}
    check:
      description: 验证热点开启后出现热点图标
      manual:
        actor: QA
        target: DUT屏幕
        action: 验证热点开启后出现热点图标
      automation:
        api: check_hotspot_enabled
        params: {}

