_schema: test_case_spec_v1

id: Wifi_Func_021
name: Restart with wifi off 后验证WiFi开关保持关闭
priority: P1
domain: wifi
environment: wifi_router_lan
description: |
  Connect AP

_source:
  file: wifi&BT_testcase_v1.1.xlsx
  sheet: Wifi_function
  case_id: Wifi_Func_021
preconditions:
  - description: 1. 平台已正常启动并进入系统 2. WiFi 模块已初始化
    manual: QA 人工确认
    automation:
      api: check_wifi_connected

steps:
  - operation:
      description: 断开网络连接(wifi/ethernet), 模拟Restart with wifi off场景
      manual:
        actor: QA
        target: DUT
        action: 断开网络连接(wifi/ethernet), 模拟Restart with wifi off场景
      api: step_disconnect_network
      params: {}
    check:
      description: "验证wifi default switch: Wifi switch default status is on, 但此处需要验证重启后WiFi开关保持关闭状态"
      manual:
        actor: QA
        target: DUT屏幕
        action: "验证wifi default switch: Wifi switch default status is on, 但此处需要验证重启后WiFi开关保持关闭状态"
      api: check_wifi_state
      params: {}

